/* --------------------
 * livepack module
 * External functions
 * These functions are used within output.
 * ------------------*/

'use strict';

// Exports

module.exports = {
	createArguments: (0, function() { return arguments; }) // eslint-disable-line prefer-rest-params
};
